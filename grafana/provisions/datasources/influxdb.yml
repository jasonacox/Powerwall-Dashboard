apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    isDefault: true
    database: powerwall
    jsonData:
      dbName: powerwall
      timeInterval: 5s
